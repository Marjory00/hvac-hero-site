// src/lib/types/mock_data.ts
// This defines the minimal expected shape for public components.
export interface MockQueryResult {
  id: number;
  result: string;
  // Add other essential, non-sensitive keys needed by the UI
}